天气：
key:e544ab803ee4e148744a8ed5a3055f03
接口地址：https://restapi.amap.com/v3/weather/weatherInfo
返回格式：json
请求方式：get
请求示例：https://restapi.amap.com/v3/weather/weatherInfo?city=110101&key=<用户key>
city代码对应参考：D:\Pycharm\PycharmProject\pythonProject\venv\上海旅游\Agent\AMap_adcode_citycode.xlsx

key	string	
city	string	必填	
extensions 	string	选填	气象类型
output	string	选填	返回数据格式类型，默认json



路径规划：
key：95dfa5cfda994230af9b6ab64de4b84b
项目	详情
API 服务地址（URL）	https://restapi.amap.com/v5/direction/driving?parameters
请求方式	1. 默认使用 GET 方式
2. 当参数过长导致请求失败时，需切换为 POST 方式
参数连接规则	所有参数（含必填、可选）均使用 & 符号分隔

参数分类	参数名	含义	规则说明	是否必须	缺省值	
核心认证参数	key	高德 Key（API 访问凭证）	需在高德地图官网申请 Web 服务 API 类型 的 Key，不可为空	是	无	
起点 / 终点参数	origin	起点经纬度	格式为 “经度，纬度”（经度在前、纬度在后），经纬度小数点后不得超过 6 位	是	无	
destination	目的地经纬度	规则同 origin（“经度，纬度”，小数点后≤6 位）	是	无	
destination_type	终点的 POI 类别	仅当用户明确知道终点 POI 类别时建议填充，用于优化路线规划	否	无	
origin_id	起点 POI ID	起点为 POI 时建议填充，可提升路线规划准确性	否	无	
destination_id	目的地 POI ID	目的地为 POI 时建议填充，可提升路线规划准确性	否	无	
路线策略参数	strategy	驾车算路策略	可选值及含义：
- 0：速度优先（仅 1 条路线）
- 1：费用优先（仅 1 条，不走收费路）
- 2：常规最快（仅 1 条，综合距离 / 耗时）
- 32：默认（高德推荐，同 APP 默认）
- 33：躲避拥堵
- 34：高速优先
- 35：不走高速
- 36：少收费
- 37：大路优先
- 38：速度最快
- 39：躲避拥堵 + 高速优先
- 40：躲避拥堵 + 不走高速
- 41：躲避拥堵 + 少收费
- 42：少收费 + 不走高速
- 43：躲避拥堵 + 少收费 + 不走高速
- 44：躲避拥堵 + 大路优先
- 45：躲避拥堵 + 速度最快	否	32	
路径约束参数	waypoints	途经点	格式为 “经纬度 1; 经纬度 2;...”（多个途经点用英文分号分隔），默认支持 1 个，最大支持 16 个	否	无	
avoidpolygons	避让区域	1. 单个区域最多 16 个顶点，多个区域用英文竖线 “	” 分隔，最大支持 32 个区域
2. 单个避让区域面积不得超过 81 平方公里，否则失效	否	无
ferry	是否使用轮渡	可选值：0（使用渡轮）、1（不使用渡轮）	否	0	
车辆相关参数	plate	车牌号码	格式如 “京 AHA322”，支持 6 位传统车牌、7 位新能源车牌（用于判断限行）	否	无	
cartype	车辆类型	可选值：0（普通燃油汽车）、1（纯电动汽车）、2（插电式混动汽车）	否	0	
返回结果参数	show_fields	返回结果控制（筛选可选字段）	1. 可指定字段需参考 “返回结果说明” 中 “show_fields” 相关字段
2. 多个字段用英文逗号 “,” 分隔
3. 未设置时仅返回基础信息类字段	否	空	
output	返回结果格式类型	仅支持 JSON 格式	否	json	
callback	回调函数名	仅当 output 设置为 JSON 时有效，值为用户自定义函数名	否	无	
安全验证参数	sig	数字签名	需参考高德官方 “数字签名获取和使用方法” 生成，用于提升请求安全性







